/** Server Component layout for the /shop route — sets metadata. */
export const metadata = {
  title       : "Shop Fragrances | LUXEURE",
  description : "Browse 500+ luxury fragrances from Dior, Tom Ford, Creed, Chanel and more. Filter by gender, brand, price, and concentration.",
};

export default function ShopLayout({ children }) {
  return children;
}
